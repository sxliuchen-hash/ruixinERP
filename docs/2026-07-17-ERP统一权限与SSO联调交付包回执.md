---
title: ERP 统一权限与 SSO 联调交付包回执
type: integration-handoff-receipt
status: e01-e12-complete-j01-pending
owner: ERP 团队
updated: 2026-07-17
source:
  branch: codex/docs-erp-integration-handoff-20260717
  commit: fd5c2eac5af797adb9f0bfe803b0ab24f97455b1
  document: docs/records/2026/2026-07-17-主项目统一权限与SSO联调交付包.md
---

# ERP 统一权限与 SSO 联调交付包回执

## 一、版本

```text
ERP 候选 Commit：d3848066ac51a9cbf44d17f75c60af7422306df2
ERP 候选 Tag：erp-unified-auth-r7-rc2（已推送；准确指向上述 Commit；不是生产正式 Tag）
ERP 候选分支：codex/erp-unified-auth-r7-rc1、feature/erp-unified-auth-r7-rc2
GitHub Actions：CI 29552073120，四项 Job 全部成功
工作区是否干净：是（2026-07-17 独立核验）
```

本轮本地门禁：

```text
npm run test:ci
npm run test:delivery-safety
npm run test:frontend-auth-static
npm run test:integration:ci（隔离 MySQL）
npm run lint（backend/frontend）
npm audit --omit=dev --audit-level=high（backend/frontend）
npm audit --audit-level=moderate（frontend）
VITE_MAIN_SYSTEM_URL=https://iptt.top npm run build
git diff --check
```

最终证据：两个远端候选分支均指向 `d3848066ac51a9cbf44d17f75c60af7422306df2`；[GitHub Actions CI 29552073120](https://github.com/sxliuchen-hash/ruixinERP/actions/runs/29552073120) 的 Frontend、Backend、Delivery Safety、Isolated MySQL Integration 四项 Job 全部成功；候选 Tag `erp-unified-auth-r7-rc2` 已推送并准确指向该 Commit。旧候选 Tag `erp-unified-auth-r7-rc1` 仍指向 `5342c56f951a80ca8e56138d68f2cdce8f7322b1`，未移动。

`erp-unified-auth-r7-rc2` 的 tagger 仍是历史占位身份，已按不可变发布原则保留，不强制移动。ERP 仓库后续提交、新候选 Tag 和生产正式 Tag 使用可审计发布身份 `sxliuchen-hash`（GitHub ID `232716846`）。

## 二、固定地址

```text
测试基础地址：https://erp-test.iptt.top
测试 SSO initiate：https://erp-test.iptt.top/sso/initiate
测试 SSO callback：https://erp-test.iptt.top/sso/callback
测试 Manifest URL：https://erp-test.iptt.top/api/v1/internal/permissions/manifest
测试 Employee provisioning URL：https://erp-test.iptt.top/api/v1/internal/provisioning/employees
生产基础地址：https://erp.iptt.top
SSO initiate：https://erp.iptt.top/sso/initiate
SSO callback：https://erp.iptt.top/sso/callback
Manifest URL：https://erp.iptt.top/api/v1/internal/permissions/manifest
Employee provisioning URL：https://erp.iptt.top/api/v1/internal/provisioning/employees
```

E-12 环境已完成：

- 负责人：ERP 项目负责人兼运维负责人（发布身份：`sxliuchen-hash`）。
- 实际完成日期：2026-07-17（原计划完成日期：2026-07-20）。
- DNS：`erp-test.iptt.top A 124.222.116.195`。
- 候选部署：`erp-unified-auth-r7-rc2@d3848066`，独立目录 `/srv/erp-r7-test`。
- 独立服务：MySQL `127.0.0.1:3307`、Redis `127.0.0.1:6381`、ERP backend `127.0.0.1:3101`；systemd 分别设置内存上限 256MB、64MB、256MB 和 CPU 配额。
- 定时任务隔离：测试实例通过启动包装器禁用企微、COS、年费、合同提醒、固定月费、专利扫描和对账定时任务，不触碰生产业务。
- HTTPS 验收：Let's Encrypt 证书、Nginx TLS/SNI、公开路径 `/api/v1/health/ready` 均通过验证。
- 接口验收：Manifest 未授权拒绝、受控测试凭证授权成功；Employee 建档未授权拒绝、首次创建成功、相同幂等键重放成功；测试数据已清理。
- 压力验收：部署后可用内存约 1.3GB，CPU 空闲约 91%～99%，无持续新增换页；主项目 PM2、MySQL 3306、Redis 6379 未重启或修改。
- 不依赖项：E-12 不依赖主项目生产环境；主项目只需在联合阶段回填其测试基础地址和环境专属值。

## 三、ERP 独立完成项

### E-01：完成

- 冻结候选：`d3848066ac51a9cbf44d17f75c60af7422306df2`；候选 Tag `erp-unified-auth-r7-rc2` 已推送并准确指向该 Commit。
- 分支：`codex/erp-unified-auth-r7-rc1`、`feature/erp-unified-auth-r7-rc2` 均已推送该 Commit。
- 门禁：后端完整 Jest、Delivery Safety、前端权限静态门禁、前后端 lint/audit/build、隔离数据库集成测试。
- 远端 CI：`29552073120`，四项 Job 全部成功；候选提交核验时工作区干净。

### E-02：完成

```text
Manifest version：1.3.0
SHA-256：766bc454653d603b3d5944cb8f5647dc20a9cc71f8f5d4a4939f684abc559646
模块：30
权限：117
唯一接口映射：192
```

证据：`backend/src/permissions/erp-permission-manifest.json`、`frontendAuthFlowStaticAudit.test.js`、`expressRouteManifestCoverage.test.js`。

### E-03：完成

- 固定端点：`GET /api/v1/internal/permissions/manifest`。
- 仅允许 `main-permission-center` 固定 Header 凭证。
- 错误、缺失、部分凭证默认拒绝；响应不包含角色/用户授权数据。
- 证据：`backend/tests/internalManifestApi.test.js`。

### E-04：完成

- 固定端点：`POST /api/v1/internal/provisioning/employees`。
- 固定 Header：`X-Main-Provision-Client-Id/Secret`，必须携带 `Idempotency-Key`。
- 相同 key/相同请求返回同一结果；相同 key/不同内容冲突；支持并发、重试和迟到结果收敛。
- 证据：`backend/tests/employeeProvisioning.test.js`。

### E-05：完成

- `/sso/initiate` 使用后端生成的 43 字符 Base64URL state。
- state 通过 `HttpOnly; SameSite=Lax; Path=/` Cookie 绑定当前浏览器；生产环境强制增加 `Secure`，即 `HttpOnly; Secure; SameSite=Lax; Path=/`。
- 只接受与构建时主项目 Origin 一致的 `/sso/continue?app=erp&state=...`。
- Playwright CLI/Chrome 自动化：固定 mock main Origin 下成功进入 `/sso/continue`；缺少构建 Origin 时前端 fail-closed。
- 证据：Playwright 2026-07-17 本机隔离会话；`ssoStateService.test.js`、`ssoStateControllerFlow.test.js`、`ssoStateFrontendAudit.test.js`。

### E-06：完成

- callback 读取后立即清除地址栏 Code/state，再调用 exchange。
- Playwright 成功路径：`urlCleanBeforeExchange=true`，建立 `main_sso` 会话，permissionVersion=7，进入授权 dashboard。
- 第二浏览器复制同一 callback：HTTP 401，错误页显示，本地 `erp_token` 不存在。
- 第一浏览器重放同一 callback：HTTP 401，旧 Token 被清除。
- 证据：Playwright 2026-07-17 两个隔离 Chrome 会话；`ssoStateControllerFlow.test.js`、`ssoExchange.test.js`。

### E-07：完成

- 严格验证 RS256、kid、iss、aud、sub、嵌套 user.id、iat、exp、jti、最大生命周期和入口权限。
- 主项目 assertion 的固定 issuer 为 `patent-notice-system`；测试或代码中的 `iss: 'erp'` 仅用于 ERP 兑换后签发的本地会话 Token，不是主项目 assertion 配置。
- 拒绝无 kid、未知 kid、错误签名、过期、未来 iat、宽松 claim、无入口权限和非法角色。
- jti 使用 Redis SET NX 防重放；Redis 异常 fail-closed。
- 证据：`backend/tests/ssoExchange.test.js`、`authCompatibility.test.js`。

### E-08：完成

- ERP 后端执行接口权限、scope、数据属主和高风险实时版本检查。
- service-only 固定为合同确认、收款确认、报销审批，人工用户包括 admin 均不可获得。
- 工资导出同时要求 `erp.payroll.view` 和 `erp.payroll.export`。
- 证据：`rolePermissionInteraction.test.js`、`payrollService.test.js`、Manifest/Express 路由双向覆盖测试及完整 Jest。

### E-09：完成

- permissionVersion 普通缓存复核、高风险实时复核、版本变化清缓存和旧 session 失效已实现。
- 业务调用、team scope 和文件票据均不接受过期版本；下游不可用不扩大权限。
- 证据：`permissionVersion.test.js`、`authCompatibility.test.js`、完整 Jest。

### E-10：完成

- 五项迁移均提供显式、幂等、冲突 fail-closed 入口。
- 本机隔离 MySQL 8.0：五项迁移连续执行两次均通过；真实集成测试 1 suite / 9 tests 通过。
- 修复 MySQL 8.0/mysql2 对生成表达式反斜杠转义导致的误判，并增加真实格式回归测试。
- GitHub CI 已补齐 Employee、企微审批单号、业绩确认周期三项迁移的双次幂等验证；推送后由固定 MySQL 8.4 digest 复核。
- 本地隔离统一认证生产预检：0 失败；Manifest、数据库唯一约束、Redis 实连均通过，只有 hybrid 灰度提示。
- 备份、COS 回执、恢复 smoke、release precheck、ready 和回滚脚本已有 Delivery Safety 测试。

生产备份字段当前不填写实际值：

```text
备份命令退出码/大小/SHA-256：未执行；原因是尚未进入生产维护窗口
异机副本位置编号：未执行；负责人为 ERP 运维；完成条件是生产停写后的最终备份
```

### E-11：完成

```text
main-permission-center：active，purpose=manifest-read
main-provisioning：active，purpose=employee-provisioning
受控材料编号：erp-inbound-20260717025601-0526203a-bbc2-471e-99bc-de05741be6fb
main-permission-center Secret SHA-256：eb9ee485ed6a2a77c0edb0966efc43ff3873250f0fe41a4eb61953f8a6b7ae3c
main-provisioning Secret SHA-256：2544cf4af5aa59f6515bf0f679b71a2ddc7f6a0462ef72ad3859ff9555f882d6
```

- 两套 Secret 由 ERP CSPRNG 独立生成，未进入 Git、聊天、普通文档或日志。
- 主项目领取包已于 2026-07-17 准备完成；材料编号和两枚 SHA-256 指纹已复核一致，Git ignore 校验通过。
- 受控领取包已移除继承 ACL，仅保留本机用户 `sx_li` 和 SYSTEM FullControl。
- 生成工具拒绝相对路径、覆盖现有材料、重复 Secret 和未知参数；终端只输出材料编号、Client ID 和指纹。
- 交付负责人：ERP 项目负责人 `sxliuchen-hash`；交付状态：受控包准备完成，可立即领取。
- Secret 仅通过受控渠道交付；本文不记录本机路径和明文。

### E-12：完成

- 生产基础地址、initiate、callback、Manifest、provisioning URL 已固定。
- 候选 Commit、远端分支、CI 和候选 Tag 的最终证据已固定，见本文第一节。
- ERP 独立测试 HTTPS、DNS、TLS、Nginx、候选部署、MySQL、Redis 和四个固定端点均已完成并通过验收。
- 负责人：ERP 项目负责人兼运维负责人（发布身份：`sxliuchen-hash`）；实际完成日期：2026-07-17。
- 证据：服务器本机和本机公网路径 HTTPS ready；Manifest/Employee 认证与幂等 smoke 通过；隔离资源压力在限额内；临时凭证和测试数据未写入回执。

## 四、密钥与凭证

```text
main-permission-center 状态：主项目已受控领取并核验指纹
main-provisioning 状态：主项目已受控领取并核验指纹
主项目出站材料编号：main-outbound-20260717091448-bfefab5b-202a-44f8-8e18-a166486bfd88
erp-sso Secret SHA-256：74b2137018d5b2d525e773b52e99820ae391817b32689aabd4b097686848763e
erp-main-api Secret SHA-256：e674ea0fc515e6bcd16c674f092fab3e3bee4f2e93876ca12a21b59a25e0b80a
主项目 active kid：main-test-20260717-01
主项目公钥文件 SHA-256：f1a53e55f95fd2265402b2bee1837f4079e65c858176ff172e2f4fbc77dc4c39
主项目公钥 SPKI DER SHA-256：119823b2d6c6bf5115a386214fafc0b187d308eefdc21fdef23a928b34926aef
previous key：无（active-only）
ERP 两套入站凭证交付负责人：sxliuchen-hash
主项目两套凭证及签名材料状态：ERP 已受控领取、核验并写入隔离测试配置
```

主项目出站材料接收与部署已完成：

- ERP 实算两套 Client Secret、公钥文件及 SPKI DER 指纹，与主项目回执全部一致；RSA 位数为 3072。
- Client ID、issuer=`patent-notice-system`、audience=`erp`、active kid 和 active-only 状态核对通过。
- 真实材料已原子写入 `/srv/erp-r7-test/shared/backend.env` 和独立公钥文件，权限均为 `600`、所有者为 `erp_r7_test`。
- 生产级统一认证 preflight 0 失败通过；Manifest `1.3.0` 为 117 权限、192 路由；数据库和 Redis 实连检查通过。
- `erp-r7-test-backend.service` 已重启并保持 active，HTTPS ready 连续 3/3 通过。

## 五、数据库与回滚

```text
ERP 迁移列表：
  1. migrate:payroll-schema
  2. migrate:system-settings
  3. migrate:employee-user-id-unique
  4. migrate:wechat-sp-no-unique
  5. migrate:performance-confirmed-period-unique
迁移隔离验证结果：MySQL 8.0 五项双次幂等通过；集成 9/9
备份命令退出码/大小/SHA-256：生产维护窗口执行，当前未执行
异机副本位置编号：生产维护窗口执行，当前未确定
生产回滚 Commit：ca26bf3986160641d905736d97699ffbb72bc87e
旧代码候选 Tag：erp-unified-auth-r7-rc1@5342c56f951a80ca8e56138d68f2cdce8f7322b1
```

生产备份和异机副本不属于静态 E-01～E-12 的执行结果；必须在已授权的生产维护窗口停写后生成，不得用本机隔离库工件代填。

## 六、联合联调

```text
ERP 联调负责人：sxliuchen-hash
ERP 运维负责人：项目负责人本人（发布身份：sxliuchen-hash）
建议可用联调窗口：2026-07-18 10:00～18:00（UTC+8）
```

联合联调的凭证配置阶段已执行：

- 四套凭证交叉用途、ERP 真实配置、active 公钥和生产级 preflight 已完成，不再等待或重复确认静态契约。
- 2026-07-17 使用真实 `erp-sso` 和 `erp-main-api` 凭证探测 `https://iptt.top`，`exchange`、`team-scope`、`permission-version` 三个 R7 后端端点均返回 HTTP 404；相同路径与主项目本地 R7 代码完全一致。
- 当前唯一的链路阻断是 `iptt.top` 运行版本尚未暴露上述 R7 后端路由；ERP 已指向约定地址并保持 `erp-test.iptt.top` 稳定。
- 主项目路由部署后，使用一个正式测试 `users.id` 即可直接执行 Code 兑换、assertion 验签、team scope 和撤权失效测试。

下一步仅需两个运行条件，不再补交静态材料：

1. 在约定主项目地址部署已经存在于 R7 代码中的三个内部路由，确保真实凭证请求不再返回 404；
2. 指定一个正式测试 `users.id`，用于正常 SSO 和 scope 场景。

本文不再请求主项目重复提供已经固定的接口、Header、权限、角色、scope、请求响应和测试规则。只有 ERP 测试 HTTPS 环境、主项目 M-01～M-08、四套受控凭证、公钥及指纹、测试账号矩阵全部完成后，双方才可从 `J-01` 开始；此前不得宣布已经进入 J-01 或已经具备上线条件。

## 七、状态边界

- 本回执证明 ERP 已独立完成 E-01～E-12 的代码、测试、受控材料准备和独立测试 HTTPS 环境；这不代表双方已经进入 J-01。
- 本回执不代表主项目 R7 已部署、双方已经联通、ERP 已生产部署或生产 SSO 已开启。
- `release/20260715.2` 不是本次生产目标；`erp-unified-auth-r7-rc2` 仅为候选 Tag。
- 四套凭证、公钥指纹、ERP 真实受控配置和生产级 preflight 已完成；主项目三个 R7 内部路由仍返回 404，且正式测试账号尚未指定，因此完整 SSO 链路尚未通过。
- 主项目路由可达后直接执行联合链路测试；全部通过后才允许创建另行命名的生产正式 Tag。
